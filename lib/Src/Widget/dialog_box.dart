import 'package:flutter/material.dart';

class CustomeDialogBox extends StatelessWidget {
  const CustomeDialogBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: AlertDialog(
      actions: [],
    ));
  }
}
